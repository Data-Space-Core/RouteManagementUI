apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-management-ui
  namespace: route-management-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: route-management-ui
  template:
    metadata:
      labels:
        app: route-management-ui
    spec:
      serviceAccountName: route-management-ui
      containers:
        - name: route-management-ui
          image: ghcr.io/data-space-core/route-management-ui:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: route-management-ui-config
            - secretRef:
                name: route-management-ui-secret
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
